#include "foretias_core.h"

ForetiasResult foretias_hash_blake3(const uint8_t* data, size_t len, ForetiasHash32* out) {
    (void)data;
    (void)len;
    (void)out;
    return FORETIAS_ERR_UNSUPPORTED;
}
