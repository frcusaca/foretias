#include "fortias_core.h"

FortiasResult fortias_hash_blake3(const uint8_t* data, size_t len, FortiasHash32* out) {
    (void)data;
    (void)len;
    (void)out;
    return FORTIAS_ERR_UNSUPPORTED;
}
