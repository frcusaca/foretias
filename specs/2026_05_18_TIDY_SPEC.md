# 2026-05-18 Tidy Up — Specification

**Version:** v0.2
**Status:** Completed — all tasks merged to alpha (2026-05-18)
**Date:** 2026-05-18
**Scope:** Step — document updates + execute two pending specs

---

## Overview

This specification consolidates five tasks to bring documentation in sync with the current codebase state and execute two pending specs that are ready for implementation.

---

## Task 1 — Update AGENTS.md

**Goal:** Reflect the current 3-crate architecture after the `RUST_THREE_LEVEL_INSTANTIATION` refactor.

### What Changed in Code

The `foretias-node` crate was split into two crates:
- **`foretias-client`** — Thin client library (Standalone, PtP, P2P levels), `Foretias`, `ForetiasError`, noise PtP, calendar, mirror
- **`foretias-server`** — Server, JSON-RPC, calendar store, Communerd (P2P), CLI binary

The workspace (`p2p/Cargo.toml`) now has 3 crates: `core-engine`, `foretias-client`, `foretias-server`.

### Required Updates to AGENTS.md

| Section | Current (wrong) | Update to |
|---------|----------------|-----------|
| Project structure tree | `foretias-node/` with server+CLI | Split into `foretias-client/` and `foretias-server/` |
| CLI table | `p2p/foretias-node/src/main.rs` | `p2p/foretias-server/src/main.rs` |
| Architecture layers diagram | `foretias-node` box | `foretias-server` box |
| Build commands | `cargo build -p foretias-node` | `cargo build -p foretias-server` |
| Test commands | `cargo test -p foretias-node` | `cargo test -p foretias-server` |
| Dependency chain | "foretias-node" | "foretias-client → foretias-server" |
| README reference | `foretias-node` mentions | `foretias-server` |

### Files to Update

- [x](2026-05-18 09:00) `AGENTS.md` — Project structure, CLI table, architecture diagram, build/test commands

---

## Task 2 — Update README.md

**Goal:** Remove stale references, ensure accuracy.

### Current State

README.md is largely correct — already references Rust CLI only, no Python/Scala docs present.

### Verify and Update

| Check | Status | Action |
|-------|--------|--------|
| Python documentation present | ❌ None | No change |
| Scala documentation present | ❌ None | No change |
| `foretias-node` references | ✅ Found 1 | Update to `foretias-server` |
| Build commands current | ✅ Correct | No change |
| CLI commands current | ✅ Correct | No change |

### Files to Update

- [ ] `README.md` — Update `foretias-node` → `foretias-server` where mentioned

---

## Task 3 — Update HOWTO.md

**Goal:** Ensure examples work with the current codebase.

### Current State

HOWTO.md uses the `foretias` binary and `start_local_peers.py` peer manager. The CLI interface has NOT changed — `serve`, `stamp`, `verify`, `prove-verification`, `inspect-attestations` commands are unchanged.

### Assessment

| Element | Changed? | Action |
|---------|----------|--------|
| `foretias` binary path | No — still `p2p/target/debug/foretias` | No change |
| CLI flags/commands | No | No change |
| Peer manager (`start_local_peers.py`) | Exists and works | No change |
| `foretias-node` references in prose | Check for any | Update if found |

### Files to Update

- [ ] `HOWTO.md` — Review for any `foretias-node` references; update if found. Otherwise no change needed.

---

## Task 4 — Execute CHRONONCHAIN_NAMING_SPEC

**Goal:** Rename `TickRecord` → `ChrononRecord`, `tick_number` → `chronon_number` across code and documentation.

### Current State

- **Code:** `TickRecord` exists in 15 files, `tick_number` in 508 occurrences
- **`ChrononRecord`:** Does not exist anywhere
- **Spec:** `CHRONONCHAIN_NAMING_SPEC.md` — ready for implementation
- **Plan:** No plan file exists — create `CHRONONCHAIN_NAMING_PLAN.md`

### Scope

This is a **terminology normalization** task. The spec defines:
- `TickRecord` → `ChrononRecord`
- `tick_number` → `chronon_number` (with serde compat alias)
- Doc comment updates throughout
- JSON-RPC parameter `cal_tick_start` → `cal_chronon_start`
- Spec documentation updates

### Approach

Create a plan (`CHRONONCHAIN_NAMING_PLAN.md`) with worktree + branch, then execute phases:
1. **Phase 0** — Core engine rename (`types.rs`, `tick.rs`, `calendar.rs`, `mod.rs`)
2. **Phase 1** — Server updates (`handlers.rs`, `jsonrpc.rs`, `encrypted_jsonl.rs`)
3. **Phase 2** — Client updates (`foretias.rs`, `calendar/`)
4. **Phase 3** — Spec/doc updates (`foretias-v1.md`, `FORETIAS_0_OVERVIEW.md`, etc.)
5. **Phase 4** — Tests + merge

### Files to Update

- [ ] Create `CHRONONCHAIN_NAMING_PLAN.md`
- [ ] Execute plan (worktree + branch + phases)
- [ ] Merge to alpha
- [ ] Finalize spec (mark status "Completed")

---

## Task 5 — Finalize TBID_SPHINCS_KEY_SPEC

**Goal:** Verify TBID spec is already complete and ensure proper finalization.

### Current State

- **Status:** Already completed — merged to alpha (2026-05-10)
- **All M1–M26:** Checked off with timestamps
- **Branch:** `tbid-dual-key-v1` deleted (merged)
- **Worktree:** Deleted after merge
- **Code verified:** `Tbid` struct, `TbidSecret`, `signing_tbid.c`, `signing_tbid.rs`, genesis signing — all present

### Action Required

- [ ] Verify spec status is "Completed" (already updated in this session)
- [ ] Verify plan status is "Completed" (already updated in this session)
- [ ] No further code changes needed

---

## Verification

### Acceptance Criteria

- [ ] AGENTS.md reflects 3-crate architecture (`core-engine`, `foretias-client`, `foretias-server`)
- [ ] README.md has no stale references
- [ ] HOWTO.md examples still work (CLI unchanged)
- [ ] `TickRecord` does not exist in codebase (replaced by `ChrononRecord`)
- [ ] `chronon_number` is the field name; serde compat alias present
- [ ] All tests pass after CHRONONCHAIN_NAMING rename
- [ ] TBID spec marked completed (verified)
- [ ] All specs merged to alpha

---

## Notes

- CHRONONCHAIN_NAMING is a breaking wire-format change. Serde compat alias (`#[serde(rename = "tick_number")]`) preserves backward compatibility during transition.
- TBID_SPHINCS_KEY was previously a breaking change (16B → 96B TBID). All persisted calendars were discarded at that time.
- Python/Java bindings are backburnered — skip any references in these tasks.
